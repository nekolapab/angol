name: angol
description: heksagon wirkspeys modyulir lontcir
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  intl: ^0.20.2
  # speech_to_text: ^6.6.1
  flutter_tts: ^4.2.5
  # flutter_overlay_window: ^0.4.4
  provider: ^6.1.2
  permission_handler: ^12.0.1
  # speech_to_text: ^6.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
