import 'package:flutter/foundation.dart';

class SpetcServes extends ChangeNotifier {
  void speak(String text) {}

  // void processAngolText(String text) { final glyphs = Python.run('glyphize_text.py', {'text': text}); notifyListeners(); }
}
