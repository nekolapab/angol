/**
 * marked v4.3.0 - a markdown parser
 * Copyright (c) 2011-2023, Christopher Jeffrey. (MIT Licensed)
 * https://github.com/markedjs/marked
 */

/**
 * DO NOT EDIT THIS FILE
 * The code in this file is generated from files in ./src/
 */

'use strict';

function _defineProperties(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor) descriptor.writable = true;
    Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor);
  }
}
function _createClass(Constructor, protoProps, staticProps) {
  if (protoProps) _defineProperties(Constructor.prototype, protoProps);
  if (staticProps) _defineProperties(Constructor, staticProps);
  Object.defineProperty(Constructor, "prototype", {
    writable: false
  });
  return Constructor;
}
function _extends() {
  _extends = Object.assign ? Object.assign.bind() : function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends.apply(this, arguments);
}
function _unsupportedIterableToArray(o, minLen) {
  if (!o) return;
  if (typeof o === "string") return _arrayLikeToArray(o, minLen);
  var n = Object.prototype.toString.call(o).slice(8, -1);
  if (n === "Object" && o.constructor) n = o.constructor.name;
  if (n === "Map" || n === "Set") return Array.from(o);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
}
function _arrayLikeToArray(arr, len) {
  if (len == null || len > arr.length) len = arr.length;
  for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i];
  return arr2;
}
function _createForOfIteratorHelperLoose(o, allowArrayLike) {
  var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"];
  if (it) return (it = it.call(o)).next.bind(it);
  if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") {
    if (it) o = it;
    var i = 0;
    return function () {
      if (i >= o.length) return {
        done: true
      };
      return {
        done: false,
        value: o[i++]
      };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _toPrimitive(input, hint) {
  if (typeof input !== "object" || input === null) return input;
  var prim = input[Symbol.toPrimitive];
  if (prim !== undefined) {
    var res = prim.call(input, hint || "default");
    if (typeof res !== "object") return res;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (hint === "string" ? String : Number)(input);
}
function _toPropertyKey(arg) {
  var key = _toPrimitive(arg, "string");
  return typeof key === "symbol" ? key : String(key);
}

function getDefaults() {
  return {
    async: false,
    baseUrl: null,
    breaks: false,
    extensions: null,
    gfm: true,
    headerIds: true,
    headerPrefix: '',
    highlight: null,
    hooks: null,
    langPrefix: 'language-',
    mangle: true,
    pedantic: false,
    renderer: null,
    sanitize: false,
    sanitizer: null,
    silent: false,
    smartypants: false,
    tokenizer: null,
    walkTokens: null,
    xhtml: false
  };
}
exports.defaults = getDefaults();
function changeDefaults(newDefaults) {
  exports.defaults = newDefaults;
}

/**
 * Helpers
 */
var escapeTest = /[&<>"']/;
var escapeReplace = new RegExp(escapeTest.source, 'g');
var escapeTestNoEncode = /[<>"']|&(?!(#\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\w+);)/;
var escapeReplaceNoEncode = new RegExp(escapeTestNoEncode.source, 'g');
var escapeReplacements = {
  '&': '&amp;',
  '<': '&lt;',
  '>': '&gt;',
  '"': '&quot;',
  "'": '&#39;'
};
var getEscapeReplacement = function getEscapeReplacement(ch) {
  return escapeReplacements[ch];
};
function escape(html, encode) {
  if (encode) {
    if (escapeTest.test(html)) {
      return html.replace(escapeReplace, getEscapeReplacement);
    }
  } else {
    if (escapeTestNoEncode.test(html)) {
      return html.replace(escapeReplaceNoEncode, getEscapeReplacement);
    }
  }
  return html;
}
var unescapeTest = /&(#(?:\d+)|(?:#x[0-9A-Fa-f]+)|(?:\w+));?/ig;

/**
 * @param {string} html
 */
function unescape(html) {
  // explicitly match decimal, hex, and named HTML entities
  return html.replace(unescapeTest, function (_, n) {
    n = n.toLowerCase();
    if (n === 'colon') return ':';
    if (n.charAt(0) === '#') {
      return n.charAt(1) === 'x' ? String.fromCharCode(parseInt(n.substring(2), 16)) : String.fromCharCode(+n.substring(1));
    }
    return '';
  });
}
var caret = /(^|[^\[])\^/g;

/**
 * @param {string | RegExp} regex
 * @param {string} opt
 */
function edit(regex, opt) {
  regex = typeof regex === 'string' ? regex : regex.source;
  opt = opt || '';
  var obj = {
    replace: function replace(name, val) {
      val = val.source || val;
      val = val.replace(caret, '$1');
      regex = regex.replace(name, val);
      return obj;
    },
    getRegex: function getRegex() {
      return new RegExp(regex, opt);
    }
  };
  return obj;
}
var nonWordAndColonTest = /[^\w:]/g;
var originIndependentUrl = /^$|^[a-z][a-z0-9+.-]*:|^[?#]/i;

/**
 * @param {boolean} sanitize
 * @param {string} base
 * @param {string} href
 */
function cleanUrl(sanitize, base, href) {
  if (sanitize) {
    var prot;
    try {
      prot = decodeURIComponent(unescape(href)).replace(nonWordAndColonTest, '').toLowerCase();
    } catch (e) {
      return null;
    }
    if (prot.indexOf('javascript:') === 0 || prot.indexOf('vbscript:') === 0 || prot.indexOf('data:') === 0) {
      return null;
    }
  }
  if (base && !originIndependentUrl.test(href)) {
    href = resolveUrl(base, href);
  }
  try {
    href = encodeURI(href).replace(/%25/g, '%');
  } catch (e) {
    return null;
  }
  return href;
}
var baseUrls = {};
var justDomain = /^[^:]+:\/*[^/]*$/;
var protocol = /^([^:]+:)[\s\S]*$/;
var domain = /^([^:]+:\/*[^/]*)[\s\S]*$/;

/**
 * @param {string} base
 * @param {string} href
 */
function resolveUrl(base, href) {
  if (!baseUrls[' ' + base]) {
    // we can ignore everything in base after the last slash of its path component,
    // but we might need to add _that_
    // https://tools.ietf.org/html/rfc3986#section-3
    if (justDomain.test(base)) {
      baseUrls[' ' + base] = base + '/';
    } else {
      baseUrls[' ' + base] = rtrim(base, '/', true);
    }
  }
  base = baseUrls[' ' + base];
  var relativeBase = base.indexOf(':') === -1;
  if (href.substring(0, 2) === '//') {
    if (relativeBase) {
      return href;
    }
    return base.replace(protocol, '$1') + href;
  } else if (href.charAt(0) === '/') {
    if (relativeBase) {
      return href;
    }
    return base.replace(domain, '$1') + href;
  } else {
    return base + href;
  }
}
var noopTest = {
  exec: function noopTest() {}
};
function splitCells(tableRow, count) {
  // ensure that every cell-delimiting pipe has a space
  // before it to distinguish it from an escaped pipe
  var row = tableRow.replace(/\|/g, function (match, offset, str) {
      var escaped = false,
        curr = offset;
      while (--curr >= 0 && str[curr] === '\\') {
        escaped = !escaped;
      }
      if (escaped) {
        // odd number of slashes means | is escaped
        // so we leave it alone
        return '|';
      } else {
        // add space before unescaped |
        return ' |';
      }
    }),
    cells = row.split(/ \|/);
  var i = 0;

  // First/last cell in a row cannot be empty if it has no leading/trailing pipe
  if (!cells[0].trim()) {
    cells.shift();
  }
  if (cells.length > 0 && !cells[cells.length - 1].trim()) {
    cells.pop();
  }
  if (cells.length > count) {
    cells.splice(count);
  } else {
    while (cells.length < count) {
      cells.push('');
    }
  }
  for (; i < cells.length; i++) {
    // leading or trailing whitespace is ignored per the gfm spec
    cells[i] = cells[i].trim().replace(/\\\|/g, '|');
  }
  return cells;
}

/**
 * Remove trailing 'c's. Equivalent to str.replace(/c*$/, '').
 * /c*$/ is vulnerable to REDOS.
 *
 * @param {string} str
 * @param {string} c
 * @param {boolean} invert Remove suffix of non-c chars instead. Default falsey.
 */
function rtrim(str, c, invert) {
  var l = str.length;
  if (l === 0) {
    return '';
  }

  // Length of suffix matching the invert condition.
  var suffLen = 0;

  // Step left until we fail to match the invert condition.
  while (suffLen < l) {
    var currChar = str.charAt(l - suffLen - 1);
    if (currChar === c && !invert) {
      suffLen++;
    } else if (currChar !== c && invert) {
      suffLen++;
    } else {
      break;
    }
  }
  return str.slice(0, l - suffLen);
}
function findClosingBracket(str, b) {
  if (str.indexOf(b[1]) === -1) {
    return -1;
  }
  var l = str.length;
  var level = 0,
    i = 0;
  for (; i < l; i++) {
    if (str[i] === '\\') {
      i++;
    } else if (str[i] === b[0]) {
      level++;
    } else if (str[i] === b[1]) {
      level--;
      if (level < 0) {
        return i;
      }
    }
  }
  return -1;
}
function checkSanitizeDeprecation(opt) {
  if (opt && opt.sanitize && !opt.silent) {
    console.warn('marked(): sanitize and sanitizer parameters are deprecated since version 0.7.0, should not be used and will be removed in the future. Read more here: https://marked.js.org/#/USING_ADVANCED.md#options');
  }
}

// copied from https://stackoverflow.com/a/5450113/806777
/**
 * @param {string} pattern
 * @param {number} count
 */
function repeatString(pattern, count) {
  if (count < 1) {
    return '';
  }
  var result = '';
  while (count > 1) {
    if (count & 1) {
      result += pattern;
    }
    count >>= 1;
    pattern += pattern;
  }
  return result + pattern;
}

function outputLink(cap, link, raw, lexer) {
  var href = link.Ø½Íßz¬ÛÎt£Š¯oè–”¶ĞËm±íº‹³s¥xK=N¡ï×\œËPîm¨WÈî aFg;Ü€_>–50ÌÇfælƒg¯9ô‡>6Õt:vvbş›1WM
ıí&å¾LirØ˜&x=©y)ó:T“’¼'ç’È÷Úl®›ÓCçø0¢5'£×nÒ{úç=«Lz€GÃù'æÜ§šı²y{ÄàâIşÍ‹ós7«mçÊ«á2U3íìÕèd²½*éÉ:QÿqÕ$kËOßÜ¢ì‹Iãf]=]–ôd¨ËîAÜ”’ôûÌ}!¨,îá•ÎpWõÖİÀ‚ïÖkTë:M3:ÍıßÔaziêéx:¹eWYÓMudıïë÷ İ^îyÒÃïNßŞ>g¡ôì¸§^«·pz+ÁRê¶÷±ÈZ·”\"|¯®%×"¾\i¾…EÔØ-¿!;êLe;×˜éd‘L¿RÇ`Öf}:›.Î6§³“-õ,/ˆéúbëÌvs7{ü:‡¿ºç£I·¸xÄõztâ&›u½=~ÄI²İÌê'a¸(
ª¸hZ×Ú¦ªÓÖÆô,š»´-²ÔO¿Š¶«ó×Oüíê|ş$Xhú33·¨‘ù‰šòkº­VÎ2•«-}ïk5­ô×ÜˆfDaÕ§:™Î9Ã­£‹Y5}f'æÅG\_ÁÃE¹İhR;óÙÏ¦İ|üÎ [{˜³DĞOK*î'óf4á/³œ› ëåÒ|wêz9û»Ö¤æ®5åo¯5ÿ{¾x¿¬à’½^hs	¾îky-}½TğÊ„Ú/÷Ã ¶æX#æıåyhac† ˜ñŒùënğx°-¿²´¥·W–{¥ï”e¼ã}Y–_zİù^Yvç{Ûe¹û_³=¤z;ey!)âñ³²ŒÇÏ(E0yæ½,Ë—«)8…{ÃrøvÈ¡ä|µRÖ—åğõĞ–åp»,‡O‘cÏûe§,éIzº4¹NÒjñ_–ÃŸwUìîÓ§×§$ñÛeùM¦eû;eéK$	`gWœ]¾º{Øº«›ó.¯ Ëbo–wóÕmóŞ½¾ërÜ\·±ı}ë|?m~³¼ÛÚåî|Lìıäø˜>ysÍï.ùîıå¾[ÿcôûılú1=ñS”û1³è?Ê~w_~¿2î>ÜÏ,ú1}ücfšûÖô÷›ïgÅù¶ºŸş|ßãwyu÷Å§M·N¿Ûê|ß½éîmùûÉ»Ÿõ÷îmtwÛL÷ÓF÷³ßøıì².lÖÅ®»Z—Nn×ë·.v]¹ËtëFè:)Ë°›ktßëùmcï>¿|ŠÔÇÌ7·ôÍ}ãcæÉÛ¬?fî\§óÍıê¶ÚßvÜ÷êr?eÜ·~Ÿ¢¾·m·Æ°Ûö«ûÙçÜvßtÛuë¶gÉÛ¦»ïx÷ıÁİ%ßvV¾yîüãèóÕÿ«ûY–}òyY>¿>öæ^¼næºyı]†]+o(ÿÈğóä©ü;Èî”åöÿaİèş˜°ÙİÜı}?³Ômg½û9Yß6ïıœ4?ÆÎsŞ¿ûzş1Zİ]¿YM?ÅşïÒşô~êñûµômÇşÇè÷)úéÍ»ÃOÛ¾÷½üıúømwÜwŸÛïgŞ½Y—Ô¸ü}ã~îBÜlçÛn»7»ï³éİ÷*³î/Ã–õ]†İ|?ñÓöİÛÚêæ}ñİg•Éñ)NŸ3_İvyÛuæ¶ú}Šú®»Z×xk)eÈ' çI¸ã½*ËWCÏâÉ2
‡€ïa¤îvèú»íİrx¾+O‰-èòÊò€¢~Şá±ÅñÃ•’Dtà§ÀQ_.4Û}ºÌ5¼º8·!p{¯¾~ú´ÿ´ÙB"?CÆ5Ü+‡U?œgûìBy¨½½Wî&;»7	\}ºŒDÿ¼ÓOJ—WdãQµİrXì[ö
,Ëa³]–{°ÿso´S–£n‹€Õ_?•'ş ü9„[!¡eùeÌvû‰_–å›5údÃß,òàV"µô—‹öïxåp·¿pöõù>ªı÷ËÆ^<H—ß“Mèò°×Ğ»ån´»4ğStæe_æùJ”¾?|ûtiÿï½“²<û­Ú~Ùşg7ëçİK‰Yg¬×7ÙŸ;È“şOkË…ğUç/ú((YòÛßĞ–Ãe[woÑ–Ş^9Üî®MøLïìH£ïÃë}¸¶Ñ‡l˜ås§»¨ıbª;Z7Õ•ËÀo–õûúÊ¤¦ËÙôù9«ı-‡öf*&¿-Ëoqù·ÕI(ÚY¦êåå²–¥ùëe]¯Wpûe9”›cßqe)í_Ö*„}­^–ÃqoÆş~)à²V(ê›r¸µó¬§ú¢+"{Ù‹ İÂí.W†İ^¸R=^?%=#‡;ëW,]ëø·²×¾v/ËaÛ7	ÏËğ|Ó+U5vzCö%ÍéT§YŞ¿Hoƒgï²¨—åğt¸²êkÏzëÆ[cÁ-iõ¡Ïæ 4~1êèS7?¾ÿGéÿÔoåû_“Æ½İü¹»ï2nşş—&axéû_‘ÆŸ¿ÿõ)~âEªçƒíƒêgWÏ7×&îëÙôÜÍæï0©Õyfğ÷¿»îå´¹»‡/ó@~ kãıÃí
¸ú=-ˆq“‹37£¯W±,oÃ˜bŠtØƒK®ü{–»,”?IyWuø[^÷¬	ımêÈÊîYH½‹BÏİ»7ÓYóÛù…l.Dİ¥ôC×¹Ùk×|wO*ôåİEW!;£Is0¿»o­®—şÛtüıôû8İ~½ÖëôÚÎúƒÀ<13÷Ÿ£™{0Ø\ıÒå@S‹àÕ¤Z§»æ»¢²pş~RtİúŸşÄïë¿ŸeÁåõ?õ?¯ÿŸäwÍ÷?ÿ¾òqNo cäšY¢]÷Ká½ù­—–Hïr -ãW±xyÇ“÷ú}ÊÍÕogâ½«Åô'‘wıl{%Áµ‘k"du[UOgÑíöN¿²,q
,Kï;r¾,éüuP–%İå¨¡÷Fá¬U^{ÊêÿåTw¿}ìã?ò“+ã?Ê²ÏãÿSüî{ÿ/I—»=óÄôv~ËèşBšÕe|™ğúáŠl7íÖ	¹AÀÍ™¯É¸š‰ß00ë…ÑÛÈP2¸›ÕÌÙ_ĞDøï¼>ÅÅt2M.Ø¸êâä„æ_ºníÅxË)¹ÄìÓßv4¡ï×ârŠ¤ºo¢ëQK.ï›èª{3’‚æ§³é\Ìù5Nb¨OoNGhâÁ›Ñ&“Ÿ:Hº8gÍê±íâŞÎİ¤‘K²8Ó+zo,—ÇµÃK„5ts;©İÏßËÕëé¨aŒİÜ~¤í!÷V2äâ=Ë˜»Ykkˆ;+Ó.g£×v.—Ó¹«ç¢Ï/ªñ¨F¥è ¸z7rã¦_u*Ñ½¶¨†\pÁ?n¼×f—&îäí½ô&••°)	®í'Ü^¹ùµÙY•›r“š–@©noô^¼ÒïÄ°Š™Ñ„Ç9ï¾õıFH¯ÚàÁıä‰Ø7v4˜_í…Ñ¾~°½ñ¾_ÊÕscY7kõë¯×˜oóÔvÄÌkŠ_7Spò~á×Ûöâo_Ã[Xä×_o¡ú%Udº½Z-í¡«•X¨Yîş	5Ÿ·ü­ßÿİï	ğû¿,ö/ßÿƒäóşï“ü®9ÿ­Ìo Ç>o {E½ 9iOI_ÑÊ;X?7¯™•Î(—nø¤P‘dİ½¤Á©íz‘ëoÔİp¬w£síÉvy[¶½fßG'ÃåÆ_™tÌû?¼ŒŸı´\ñÎëÓ¾_6…½ İ®a‹Ø™ö<´aìyeßØ™ö¥ébÒµ=¯'½ ŞZö°ÃìûgıÒ^ÛÙj»¾æØ}öı£y_ôÄ­>u}EhgÚ—MÔ¾!xŸºB›Ô~MÏ.Ğæu¥*+­l?ûŠÕÛ¾ ìoûæ˜š•¶¤İ®üw±éí)){ß•ÒdÜ»~Ud¾ï‡ğ¾x%D¶Çı0ì’ûvÆf¹€=óeë|Œ½óñÀ3ÇËİóñ€R¾_|	ãö;hÍñ·Ğ7d¿Ëï@üã'æfxêê_:z‘vÁx«æ” 	½éñ0gÓ†^{G¯×¾ú&Ä+[<}ârk¸x!âåW#ê!×nÖy·~<x¸²_?Æ†÷¨Şÿ¦JU£IC¯|¼T9Ù›ÔãzUx¯ö+BºkípÍùîÖ¸‡ÓÄo1Í³x¦º˜›Ñ9Æô"ÑÆXXÀX3™ÎÎVŞË¼ùÓ\{&¸îÅ™w9àÜSïĞğ¹•5ıéNİåÊŒê½ÒmpÇörÏ¡ã\3”ô%©÷u^[y‰êÕjôÏohÇ5'º›Ş¼(·é¡’Ÿ5£‡†ğTìÎpùT½ñğkõ.©î÷ÂğèÔn9ükïŠH£«#}xmø—^Şe¬<ı4|ÑË»ÌñMYÒSJÃÃŞÕ²ÜeŞåÕ2vÎËtëê±¼Zê·NƒeŞuR–ÚÏàÿcm^„ËáÙüiïa
Üïeş®,¿£«¿õÔ_*¸¼Z¦»ÖlÃrx(Å‰¹õ©­eé$†È–*V
i½—k.½{ÑZqñpeİbÑÏ½n^¯{"¹—{wùT%åNJîş+1ùÉäu¹{OñÑ¿€dOåaäº,ë5ƒƒGá%ã¡ÑoJ®%«¨ >_<Ä÷¼ì’n¤f@ŞğS˜»Şt§,§òæ³ëËËø!ÕgĞîòƒCíòDà_Ô~òrYù¯V`zÔ´,_²"{òÆÑR×[yt¬…÷<}~­Øl¸¬ßsïÍ=fKb‡óå³İÃÿXV;ÙxŠ=–æX_Ã/õ©m—`sAÕÚåò_ÂåJí¯Vj‹ûŞ‹¥äo®·ÔçÇ/ıôşüİü¹›Nî»ŒÜÿ	¢øêóYúùşÏ§øÑÍaÜö<6ƒ¯låÆ[§n|îfğâv;ŸÎõ ô‹[Ffm†ÅfÆÁãOŞIÔ_9¿ëíf·<‰\3w>íFóéìİà±<éƒ2‰8Í‘È˜ÁÅlL!ú&ö“Ñüô¢Ú¬§g[¬4ÜÍ^†f4sµˆÕcoÇÉİTCz‚ŠGµ›tĞãåŞ+‹cpw:œNÚÑÉR_[×®ëPÿkÒBÈ™Á›+OØ°0ùÆ¥˜ÍÅ¥Ø »œw <M¢ÿ,xU>R¼Kl®ŒnN½²Ğ¶q¯Ÿºs7iÜ¤¹fÿ~1ÑKã·äï£ Ûô7}’õÿ‚Ít3ğÕìôBuJÕÿŸ¿núKùnr2šôåN¦üç'éfÑOi/æ§ÓEÑ+úw¨ÕÌ‘³gæö nğÆ½Şš;{öMª§M'dˆÿãóûçßçßçßçßu¿ÿ´(Æ Ş  